# Development Environment Variables for EarthGazer
# Copy this file to .env and fill in your values (optional - defaults will work)

# Data Storage Path
# Directory where satellite imagery data will be stored during development
# Default: ../data (relative to .devcontainer directory, maps to /workspaces/earthgazer/data)
# Example for custom path: /custom/path/to/data
# Example for different location: ~/earthgazer-dev-data
# DATA_PATH=../data

# Note: In development, the workspace is mounted at /workspaces
# The data directory will be accessible at /workspaces/earthgazer/data inside containers
